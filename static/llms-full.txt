---
url: 'https://burger-api.com/docs/overview'
---

# Burger API - Modern API Framework for Bun

Burger API is a modern, high-performance API framework built on Bun.js. It combines the simplicity of file-based routing with powerful features like lifecycle hooks, Zod-based schema validation, and automatic OpenAPI generation.

## Key Features

- ⚡ **Bun-Native Performance** - Leverages Bun's high-performance HTTP server
- 📁 **File-Based Routing** - Automatically registers API routes from your file structure
- 🚀 **Optimized Hooks** - Specialized fast paths for 0, 1, 2, and 3+ hooks
- ✅ **Type-Safe Validation** - Utilizes Zod for request validation with full type safety
- 📚 **Automatic OpenAPI Generation** - Generates complete OpenAPI 3.0 specifications
- 🔍 **Swagger UI Integration** - Out-of-the-box Swagger UI endpoint for interactive API docs
- 🎯 **Developer Friendly** - Simple, clear hook patterns that are easy to understand

---
url: 'https://burger-api.com/docs/core/burger-class'
---

# Burger Class

The `Burger` class is the main entry point for creating a Burger API application.

## Constructor

```typescript
import { Burger } from 'burger-api';

const app = new Burger(options: ServerOptions);
```

### ServerOptions Interface

```typescript
interface ServerOptions {
    // API Routing
    apiDir?: string;              // Directory path to load API routes from
    apiPrefix?: string;           // Prefix for API routes (default: 'api')
    
    // Page Routing
    pageDir?: string;              // Directory path to load page routes from
    pagePrefix?: string;           // Prefix for page routes (default: '/')
    
    // Hooks
    globalHooks?: Record<string, unknown>; // Pre-resolved global hooks module (production builds); src/hooks.ts is auto-discovered in dev
    
    // OpenAPI Documentation
    title?: string;                // API title for OpenAPI docs
    description?: string;           // API description
    version?: string;              // API version
    
    // Debugging
    debug?: boolean;               // Enable debug mode (default: false)

    // Pre-built routes (production build mode)
    apiRoutes?: RouteDefinition[]; // Use pre-built API routes (skip runtime scanning)
    pageRoutes?: PageDefinition[]; // Use pre-built page routes (skip runtime scanning)
    
    // Server Options (extends Bun's ServeOptions)
    hostname?: string;             // Hostname to bind to
    // ... other Bun server options
}
```

## Methods

### serve(port, callback?)

Starts the server and begins listening for incoming requests.

```typescript
await app.serve(4000);
await app.serve(4000, () => {
    console.log('Server started!');
});
```

**Parameters:**
- `port` (number, default: 4000) - The port number to listen on
- `callback?` (function) - Optional callback executed when server starts

**Returns:** Promise<void>

---
url: 'https://burger-api.com/docs/core/types'
---

# Type Definitions

## BurgerContext

The shared request context your handlers receive as `ctx`. It is **not** a `Request` subclass; the raw request is available as `ctx.request` and the standard surface is delegated.

```typescript
interface BurgerContext<Validated = DefaultValidated> {
    /**
     * URL parameters extracted from the request path.
     * Example: /users/:id → { id: '123' }
     */
    params?: Record<string, string>;
    
    /**
     * Validated data from Zod schemas.
     * Contains validated params, query, headers, cookies, and body.
     */
    validated?: Validated;
    
    /**
     * Wildcard parameters for wildcard routes.
     * Example: /files/[...] → ['path', 'to', 'file.txt']
     */
    wildcardParams?: string[];
    
    /**
     * Response mutations (status, headers), applied once when the response leaves.
     */
    set?: ContextSet;
    
    // Delegated request surface: ctx.request, ctx.headers, ctx.json(),
    // ctx.text(), ctx.query, ctx.route, ctx.config, ctx.services
}
```

## Hook

A hook function that processes HTTP requests.

```typescript
type Hook = (ctx: BurgerContext) => unknown;
```

**Return Types:**
- `undefined` - Continue to next hook/handler
- `Response` - Stop and send this response immediately
- `Function` - Transform the final response after handler runs

```typescript
type BurgerNext = 
    | Response
    | ((response: Response) => Promise<Response>)
    | undefined;
```

## ErrorHook

An error-path interceptor hook. Runs when the pipeline throws (beforeRoute, handler, afterRoute, mapResponse). Returns a `Response` to handle the error, or `undefined`/`void` to let the next `onError` try.

```typescript
type ErrorHook = (
    error: Error,
    ctx: BurgerContext
) => Response | void | undefined;
```

## TransformMap

Factories keyed by the context field name to inject onto the context before the handler.

```typescript
type TransformMap = Record<string, (ctx: BurgerContext) => unknown>;
```

## RequestHandler

A request handler function that processes incoming HTTP requests.

```typescript
type RequestHandler = (
    ctx: BurgerContext
) => Promise<Response> | Response;
```

## RouteHooks

The raw hook object from a `hooks.ts` file. Every value is normalized to an array when the plan is built.

```typescript
interface RouteHooks {
    onRequest?: Hook | Hook[];        // App-level only (before routing)
    beforeRoute?: Hook | Hook[];
    afterRoute?: Hook | Hook[];
    mapResponse?: Hook | Hook[];
    onError?: ErrorHook | ErrorHook[];
    transform?: TransformMap;
}
```

## RouteSchema

Zod schema structure for validating request data. Defined in `schema.ts` with per-method named exports.

```typescript
// schema.ts
export const GET = {                    // Per-method named export
    params?: z.ZodTypeAny;              // URL parameters
    query?: z.ZodTypeAny;               // Query string parameters
    headers?: z.ZodTypeAny;             // Request headers
    cookies?: z.ZodTypeAny;             // Cookies
    body?: z.ZodTypeAny;                // Request body
};
```

---
url: 'https://burger-api.com/docs/routing/file-based-routing'
---

# File-Based Routing

Burger API uses file-based routing where the file structure determines the API routes.

## Route File Naming

Routes are defined in files named `route.ts` within your API directory structure.

```
api/
├── route.ts                    → GET /api
├── users/
│   └── route.ts               → GET /api/users
├── users/
│   └── [id]/
│       └── route.ts           → GET /api/users/:id
└── files/
    └── [...]
        └── route.ts           → GET /api/files/*
```

## Route File Structure

Each route directory can export convention files:

1. **`route.ts`** - HTTP method handlers (GET, POST, PUT, DELETE, etc.)
2. **`schema.ts`** - Per-method Zod validation schemas (optional)
3. **`hooks.ts`** - Route-specific hooks (optional)
4. **`openapi.ts`** - OpenAPI metadata (optional)
5. **`config.ts`** - Route options (auth, cache, timeout)

```typescript
// api/users/schema.ts
import { z } from 'zod';

export const GET = {
    query: z.object({
        page: z.coerce.number().min(1).default(1),
    }),
};
```

```typescript
// api/users/hooks.ts
import type { BurgerContext } from 'burger-api';

export const beforeRoute = [
    async (ctx) => {
        console.log('Route hook');
        return undefined;
    },
];
```

```typescript
// api/users/route.ts
import type { BurgerContext } from 'burger-api';

export async function GET(ctx: BurgerContext) {
    return Response.json({ users: [] });
}
```

---
url: 'https://burger-api.com/docs/routing/static-routes'
---

# Static Routes

Static routes match exact paths without any dynamic segments.

## Example

```
api/
└── users/
    └── route.ts               → GET /api/users
```

**File:** `api/users/route.ts`
```typescript
export async function GET() {
    return Response.json({ message: 'Get all users' });
}
```

**Access:** `GET http://localhost:4000/api/users`

---
url: 'https://burger-api.com/docs/routing/dynamic-routes'
---

# Dynamic Routes

Dynamic routes use square brackets `[param]` to capture URL parameters.

## Example

```
api/
└── users/
    └── [id]/
        └── route.ts           → GET /api/users/:id
```

**File:** `api/users/[id]/route.ts`
```typescript
import type { BurgerContext } from 'burger-api';

export async function GET(ctx: BurgerContext) {
    const { id } = ctx.params || {};
    return Response.json({ userId: id });
}
```

**Access:** `GET http://localhost:4000/api/users/123`
**Result:** `{ "userId": "123" }`

## Multiple Dynamic Segments

```
api/
└── users/
    └── [userId]/
        └── posts/
            └── [postId]/
                └── route.ts  → GET /api/users/:userId/posts/:postId
```

**File:** `api/users/[userId]/posts/[postId]/route.ts`
```typescript
export async function GET(ctx: BurgerContext) {
    const { userId, postId } = ctx.params || {};
    return Response.json({ userId, postId });
}
```

---
url: 'https://burger-api.com/docs/routing/wildcard-routes'
---

# Wildcard Routes

Wildcard routes use `[...]` to match any path segments after the route.

## Example

```
api/
└── files/
    └── [...]
        └── route.ts           → GET /api/files/*
```

**File:** `api/files/[...]/route.ts`
```typescript
import type { BurgerContext } from 'burger-api';

export async function GET(ctx: BurgerContext) {
    const segments = ctx.wildcardParams || [];
    return Response.json({ path: segments });
}
```

**Access:** `GET http://localhost:4000/api/files/docs/guide/intro`
**Result:** `{ "path": ["docs", "guide", "intro"] }`

## Nested Wildcard Routes

```
api/
└── users/
    └── [userId]/
        └── [...]
            └── route.ts       → GET /api/users/:userId/*
```

**File:** `api/users/[userId]/[...]/route.ts`
```typescript
export async function GET(ctx: BurgerContext) {
    const { userId } = ctx.params || {};
    const segments = ctx.wildcardParams || [];
    return Response.json({ userId, path: segments });
}
```

**Access:** `GET http://localhost:4000/api/users/123/settings/privacy`
**Result:** `{ "userId": "123", "path": ["settings", "privacy"] }`

## Route Priority

Routes are matched in this order:
1. Static routes (highest priority)
2. Dynamic routes (`[id]`)
3. Wildcard routes (`[...]`) (lowest priority)

---
url: 'https://burger-api.com/docs/routing/route-groups'
---

# Route Groups

Route groups use parentheses `(group)` to organize routes without affecting the URL path.

## Example

```
api/
└── (admin)/
    └── users/
        └── route.ts           → GET /api/users (not /api/(admin)/users)
```

**File:** `api/(admin)/users/route.ts`
```typescript
export async function GET() {
    return Response.json({ users: [] });
}
```

**Access:** `GET http://localhost:4000/api/users`

The `(admin)` folder is ignored in the URL path but helps organize your code.

## Build-Time Scanner Constraints

For parity with runtime routing behavior, the CLI scanner enforces these rules:
- Do not mix wildcard (`[...]`) and dynamic (`[id]`) route folders at the same API directory level.
- Do not create multiple dynamic API folders in the same directory level (example: `[id]` and `[slug]` siblings).
- Do not create multiple wildcard API folders in the same directory level.
- For pages, do not create multiple dynamic page folders in the same directory level.

---
url: 'https://burger-api.com/docs/hooks/system'
---

# Hook System

Burger API's hook system is simple and powerful. Hook functions run before route handlers and can control the request flow.

## Hook Return Types

Hooks can return three types:

### 1. `undefined` - Continue

Allows the request to continue to the next hook or handler.

```typescript
const logger: Hook = async (ctx) => {
    console.log(`${ctx.request.method} ${ctx.request.url}`);
    return undefined; // Continue to next hook/handler
};
```

### 2. `Response` - Stop Early

Stops the request and sends a response immediately.

```typescript
const auth: Hook = async (ctx) => {
    const token = ctx.headers.get('Authorization');
    if (!token) {
        return Response.json({ error: 'Unauthorized' }, { status: 401 });
    }
    return undefined; // Continue if authorized
};
```

### 3. `Function` - Transform Response

Transforms the final response after the handler runs.

```typescript
const cors: Hook = async (ctx) => {
    // Continue processing
    return undefined;
    
    // But return a function to transform the response
    return (response: Response) => {
        response.headers.set('Access-Control-Allow-Origin', '*');
        return response;
    };
};
```

## Hook Execution Flow

1. Global hooks run first (in order)
2. Route-specific hooks run next (in order)
3. Validation runs (if schema is defined)
4. Route handler runs
5. "After" functions run in reverse order (response transformation)

---
url: 'https://burger-api.com/docs/hooks/global'
---

# Global Hooks

Global hooks run before every request. Define them when creating the Burger instance.

## Example

```typescript
// src/hooks.ts — global hooks run for every request
import { cors } from './ecosystem/hooks/cors/cors';
import { logger } from './ecosystem/hooks/logger/logger';

export const onRequest = [logger()];            // Runs first
export const beforeRoute = [cors({ origin: '*' })]; // Runs per request

// src/index.ts
import { Burger } from 'burger-api';

const app = new Burger({
    apiDir: './src/api',
});

await app.serve(4000);
```

## Creating Global Hooks

```typescript
import type { Hook } from 'burger-api';

export function logger(): Hook {
    return async (ctx) => {
        console.log(`${ctx.request.method} ${ctx.request.url}`);
        return undefined; // Continue
    };
}
```

---
url: 'https://burger-api.com/docs/hooks/route-specific'
---

# Route-Specific Hooks

Route-specific hooks run only for specific routes. Export them from the route file.

## Example

```typescript
// api/protected/route.ts
import type { BurgerContext } from 'burger-api';

export const beforeRoute = [
    async (ctx) => {
        const token = ctx.headers.get('Authorization');
        if (!token) {
            return Response.json({ error: 'Unauthorized' }, { status: 401 });
        }
        return undefined;
    },
];

export async function GET() {
    return Response.json({ message: 'Protected route' });
}
```

## Execution Order

1. Global hooks
2. Route-specific hooks
3. Validation (if schema exists)
4. Route handler

---
url: 'https://burger-api.com/docs/hooks/after'
---

# After Hooks (Response Transformation)

After hooks transform the final response after the handler runs. Return a function from a hook to use this feature.

## Example

```typescript
const cors: Hook = async (ctx) => {
    // Continue processing
    return undefined;
    
    // Return function to transform response
    return (response: Response) => {
        response.headers.set('Access-Control-Allow-Origin', '*');
        response.headers.set('Access-Control-Allow-Methods', 'GET, POST, PUT, DELETE');
        return response;
    };
};
```

## Multiple After Functions

If multiple hooks return functions, they execute in reverse order:

```typescript
// Hook 1: Add CORS headers
const cors: Hook = async () => {
    return (response) => {
        response.headers.set('Access-Control-Allow-Origin', '*');
        return response;
    };
};

// Hook 2: Add custom header
const customHeader: Hook = async () => {
    return (response) => {
        response.headers.set('X-Custom-Header', 'value');
        return response;
    };
};

// Execution order for after functions:
// 1. customHeader function runs first
// 2. cors function runs second (last added, first to transform)
```

---
url: 'https://burger-api.com/docs/validation/zod'
---

# Zod Validation

Burger API uses Zod for request validation. Define schemas in your route files to automatically validate params, query, and body.

## Schema Structure

```typescript
// schema.ts — per-method named exports
import { z } from 'zod';

export const GET = {
    params: z.object({ id: z.string().uuid() }),      // URL parameters
    query: z.object({ page: z.coerce.number() }),     // Query string
};

export const POST = {
    body: z.object({                                    // Request body
        name: z.string().min(1),
        email: z.string().email(),
    }),
};
```

## Validation Types

### Params Validation

Validates URL parameters from dynamic routes.

```typescript
// schema.ts
export const GET = {
    params: z.object({
        id: z.string().uuid(),
    }),
};
```

### Query Validation

Validates query string parameters.

```typescript
// schema.ts
export const GET = {
    query: z.object({
        page: z.coerce.number().min(1).default(1),
        limit: z.coerce.number().min(1).max(100).default(10),
        search: z.string().optional(),
    }),
};
```

### Body Validation

Validates JSON request body.

```typescript
// schema.ts
export const POST = {
    body: z.object({
        name: z.string().min(1, 'Name is required'),
        email: z.string().email('Invalid email'),
        age: z.number().int().min(18).max(120),
    }),
};
```

---
url: 'https://burger-api.com/docs/validation/accessing-validated-data'
---

# Accessing Validated Data

After validation, access validated data through `ctx.validated`.

## Example

```typescript
// schema.ts
import { z } from 'zod';

export const GET = {
    params: z.object({ id: z.string() }),
    query: z.object({ page: z.coerce.number() }),
};

export const POST = {
    body: z.object({
        name: z.string().min(1),
        email: z.string().email(),
    }),
};
```

```typescript
// route.ts
import type { BurgerContext } from 'burger-api';

export async function GET(ctx: BurgerContext) {
    const { id } = ctx.validated?.params || {};
    const { page } = ctx.validated?.query || {};
    return Response.json({ id, page });
}

export async function POST(ctx: BurgerContext) {
    const { name, email } = ctx.validated?.body || {};
    return Response.json({ name, email }, { status: 201 });
}
```

## Type Safety

Use TypeScript generics for full type safety:

```typescript
import type { POST } from './schema';
import type { BurgerContext } from 'burger-api';

export async function POST(
    ctx: BurgerContext<typeof POST>
) {
    // TypeScript knows the exact shape of ctx.validated.body
    const { name, email } = ctx.validated.body;
    return Response.json({ name, email });
}
```

---
url: 'https://burger-api.com/docs/validation/errors'
---

# Validation Errors

When validation fails, Burger API automatically returns a 400 response with error details.

## Error Response Format

When validation fails, Burger API returns a `422` with an RFC 9457 problem+json body. Issues are grouped by validation slot:

```json
{
    "type": "about:blank",
    "title": "Validation Error",
    "status": 422,
    "errors": {
        "params": [
            { "path": ["id"], "message": "Invalid uuid" }
        ],
        "query": [
            { "path": ["page"], "message": "Expected number, received string" }
        ],
        "body": [
            { "path": ["name"], "message": "String must contain at least 1 character(s)" },
            { "path": ["email"], "message": "Invalid email" }
        ]
    }
}
```

## Example

**Request:**
```bash
POST /api/users
Content-Type: application/json

{
    "name": "",
    "email": "invalid-email"
}
```

**Response:**
```json
{
    "type": "about:blank",
    "title": "Validation Error",
    "status": 422,
    "errors": {
        "body": [
            { "path": ["name"], "message": "String must contain at least 1 character(s)" },
            { "path": ["email"], "message": "Invalid email" }
        ]
    }
}
```

**Status Code:** 422 Unprocessable Entity

---
url: 'https://burger-api.com/docs/openapi/generation'
---

# OpenAPI Generation

Burger API automatically generates OpenAPI 3.0 specifications from your routes and Zod schemas.

## Automatic Generation

OpenAPI docs are automatically generated when you:
1. Define routes with HTTP method handlers
2. Use Zod schemas for validation
3. Add OpenAPI metadata (optional)

## Accessing OpenAPI JSON

The OpenAPI specification is available at `/openapi.json`:

```bash
GET http://localhost:4000/openapi.json
```

## Configuration

Configure OpenAPI metadata in the Burger constructor:

```typescript
const app = new Burger({
    apiDir: './src/api',
    title: 'My API',
    description: 'API for managing users and products',
    version: '1.0.0',
});
```

---
url: 'https://burger-api.com/docs/openapi/metadata'
---

# OpenAPI Metadata

Add OpenAPI metadata to your routes for better documentation.

## Example

```typescript
// openapi.ts — per-method named exports
export const GET = {
    summary: 'Get all users',
    description: 'Returns a paginated list of users',
    tags: ['Users'],
    operationId: 'getUsers',
    responses: {
        '200': {
            description: 'Successfully retrieved users',
            content: {
                'application/json': {
                    schema: {
                        type: 'object',
                        properties: {
                            users: { type: 'array' },
                        },
                    },
                },
            },
        },
        '400': { description: 'Invalid query parameters' },
    },
};

export const POST = {
    summary: 'Create a new user',
    description: 'Creates a user with the provided data',
    tags: ['Users'],
    operationId: 'createUser',
    responses: {
        '201': { description: 'User created successfully' },
        '400': { description: 'Invalid request body' },
    },
};
};
```

## Metadata Properties

- `summary` - Short description of the endpoint
- `description` - Detailed description
- `tags` - Group endpoints together
- `operationId` - Unique identifier for code generation
- `responses` - Define possible responses
- `deprecated` - Mark endpoint as deprecated

---
url: 'https://burger-api.com/docs/openapi/swagger-ui'
---

# Swagger UI

Burger API includes built-in Swagger UI for interactive API documentation.

## Accessing Swagger UI

Navigate to `/docs` in your browser:

```
http://localhost:4000/docs
```

## Features

- Interactive API testing
- Request/response examples
- Schema documentation
- Try it out functionality

Swagger UI automatically uses the OpenAPI specification generated from your routes.

---
url: 'https://burger-api.com/docs/cli/installation'
---

# CLI Installation

The Burger API CLI tool helps you create projects and manage hooks.

## Quick Install

**macOS, Linux, WSL:**
```bash
curl -fsSL https://burger-api.com/install.sh | bash
```

**Windows PowerShell:**
```powershell
irm https://burger-api.com/install.ps1 | iex
```

## Manual Installation

1. Download the executable from [GitHub Releases](https://github.com/isfhan/burger-api/releases/latest)
2. Add to PATH
3. Make executable (Linux/macOS): `chmod +x burger-api`

## Verify Installation

```bash
burger-api --version
```

---
url: 'https://burger-api.com/docs/cli/create'
---

# CLI: Create Command

Creates a new Burger API project with interactive prompts.

## Usage

```bash
burger-api create <project-name>
```

## Example

```bash
burger-api create my-api
```

## Interactive Prompts

The CLI will ask you:

1. **Do you need API routes?** (yes/no)
   - If yes:
     - API directory name (default: `api`)
     - API route prefix (default: `/api`)
     - Enable debug mode? (yes/no)

2. **Do you need Page routes?** (yes/no)
   - If yes:
     - Page directory name (default: `pages`)
     - Page route prefix (default: `/`)

## What Gets Created

- Full project structure
- TypeScript configuration
- Dependencies installed
- `burger.build.ts` (apiDir, pageDir, prefixes; used by CLI build)
- Example routes
- Ready to run!

## Project Structure

```
my-api/
├── src/
│   ├── index.ts              # Main server file
│   ├── api/                  # API routes (if enabled)
│   │   └── route.ts
│   └── pages/                # Page routes (if enabled)
│       └── index.html
├── ecosystem/
│   └── hooks/           # Ecosystem hooks
├── burger.build.ts          # Build/config (used by burger-api build)
├── package.json
├── tsconfig.json
└── .gitignore
```

---
url: 'https://burger-api.com/docs/cli/list'
---

# CLI: List Command

Shows all available hooks from the ecosystem.

## Usage

```bash
burger-api list
burger-api ls  # Alias
```

## Output

```
Available Hooks
────────────────────────────────

Name            Description
─────────────────────────────────────────────────
cors            Cross-Origin Resource Sharing
logger          Request/response logging
rate-limiter    Request rate limiting
jwt-auth        JWT authentication
api-key-auth    API key authentication
compression     Response compression
security-headers Security HTTP headers
cache           HTTP caching headers
timeout         Request timeout
body-size-limiter Request body size limits
```

## Usage Instructions

After running `list`, the CLI shows how to add hooks and plugins:

```bash
burger-api add <name>
```

Example: `burger-api add cors logger rate-limiter`

---
url: 'https://burger-api.com/docs/cli/add'
---

# CLI: Add Command

Adds one or more hooks or plugins to your project.

## Usage

```bash
burger-api add <names...>
```

## Examples

```bash
# Add a single hook
burger-api add cors

# Add multiple hooks at once
burger-api add cors logger rate-limiter

# Add plugins
burger-api add jwt-auth api-key
```

## What It Does

1. Downloads hook/plugin code from GitHub
2. Copies files to `ecosystem/hooks/` or `ecosystem/plugins/`
3. Shows you example code to use it

## After Adding

The CLI shows you exactly how to use the hook. Compose hooks in `src/hooks.ts`:

```typescript
// src/hooks.ts
import { cors } from './ecosystem/hooks/cors/cors';
import { logger } from './ecosystem/hooks/logger/logger';

export const onRequest = [logger()];
export const beforeRoute = [cors()];
```

## Hooks Location

Hooks are added to:
```
ecosystem/hooks/
├── cors/
│   ├── cors.ts
│   └── README.md
├── logger/
│   ├── logger.ts
│   └── README.md
└── ...
```

Check each hook's README for configuration options.

---
url: 'https://burger-api.com/docs/cli/dev'
---

# CLI: Dev Command

Starts a development server with hot reload (auto-restart on file changes).

## Usage

```bash
burger-api dev
burger-api dev --port 4000
burger-api dev --file src/index.ts
burger-api dev --port 8080 --file app.ts
```

## Options

- `-p, --port <port>` - Port to run on (default: 4000)
- `-f, --file <file>` - Entry file (default: `src/index.ts`)

## Features

- Hot reload - automatically restarts on file changes
- Fast startup
- Error reporting

## Example Output

```
→ Starting development server...

✓ Server running on http://localhost:4000
ℹ Press Ctrl+C to stop
  File changes will automatically restart the server
```

---
url: 'https://burger-api.com/docs/cli/build'
---

# CLI: Build Command

Bundles your project into `.build/bundle/` using build-time route discovery (AOT).

## Usage

```bash
burger-api build <file>
burger-api build index.ts
burger-api build src/index.ts --minify
burger-api build index.ts --outfile dist/app.js
burger-api build index.ts --sourcemap linked
```

## Options

- `--outfile <path>` - Output bundle path (default: `.build/bundle/app.js`)
- `--minify` - Minify the output for smaller file size
- `--sourcemap <type>` - Generate sourcemaps (`inline`, `linked`, or `none`)
- `--target <env>` - Target environment (default: `bun`)

## Example Output

```
✓ Build completed successfully!
  Bundle: .build/bundle/app.js
  Size: 42.5 KB
```

## Running the Bundle

```bash
bun .build/bundle/app.js
```

## Deployment Notes

- API-only projects: the bundle output can be treated as a single runnable JS file.
- Projects with pages/assets: deploy the full `.build/bundle/` directory.

---
url: 'https://burger-api.com/docs/cli/build-exec'
---

# CLI: Build Exec Command (burger-api build:exec)

Compiles your project to a standalone executable that runs without Bun installed.

## Usage

```bash
burger-api build:exec <file>
burger-api build:exec index.ts
burger-api build:exec index.ts --target bun-windows-x64
burger-api build:exec index.ts --target bun-linux-x64
burger-api build:exec index.ts --target bun-darwin-arm64
burger-api build:exec index.ts --outfile my-server.exe
```

## Options

- `--outfile <path>` - Output file path
- `--target <target>` - Target platform
- `--minify` - Minify the output (enabled by default)
- `--no-bytecode` - Disable bytecode compilation

## Supported Targets

- `bun-windows-x64` - Windows (64-bit)
- `bun-linux-x64` - Linux (64-bit)
- `bun-linux-arm64` - Linux (ARM 64-bit)
- `bun-darwin-x64` - macOS (Intel)
- `bun-darwin-arm64` - macOS (Apple Silicon)

## Example Output

```
✓ Compilation completed successfully!
  Executable: .build/executable/my-api.exe
  Size: 45.2 MB

  Standalone binary is ready to run.
  Run it: .build/executable/my-api.exe
```

## Use Case

Perfect for deploying your API to production servers without installing Bun or Node.js!

---
url: 'https://burger-api.com/docs/examples/complete-route'
---

# Complete Route Example

A complete example showing all route features.

```typescript
// api/products/openapi.ts
export const GET = {
    summary: 'Get all products',
    description: 'Returns a paginated list of products',
    tags: ['Products'],
    operationId: 'getProducts',
    responses: {
        '200': { description: 'Success' },
        '400': { description: 'Invalid query parameters' },
    },
};

export const POST = {
    summary: 'Create a product',
    description: 'Creates a new product',
    tags: ['Products'],
    operationId: 'createProduct',
    responses: {
        '201': { description: 'Product created' },
        '400': { description: 'Invalid request body' },
    },
};
```

```typescript
// api/products/schema.ts
import { z } from 'zod';

export const GET = {
    query: z.object({
        page: z.coerce.number().min(1).default(1),
        limit: z.coerce.number().min(1).max(100).default(10),
        search: z.string().optional(),
    }),
};

export const POST = {
    body: z.object({
        name: z.string().min(1, 'Name is required'),
        price: z.number().positive('Price must be positive'),
        category: z.enum(['food', 'drink', 'dessert']),
    }),
};
```

```typescript
// api/products/route.ts
import type { BurgerContext } from 'burger-api';

// Route-specific hooks
export const beforeRoute = [
    async (ctx) => {
        console.log(`[${ctx.request.method}] ${ctx.request.url}`);
        return undefined;
    },
];

// GET handler
export async function GET(ctx: BurgerContext) {
    const { page, limit, search } = ctx.validated?.query || {};
    
    // Mock data
    const products = [
        { id: '1', name: 'Burger', price: 9.99, category: 'food' },
        { id: '2', name: 'Cola', price: 2.99, category: 'drink' },
    ];
    
    // Filter if search provided
    let filtered = products;
    if (search) {
        filtered = products.filter(p => 
            p.name.toLowerCase().includes(search.toLowerCase())
        );
    }
    
    // Paginate
    const start = (page - 1) * limit;
    const paginated = filtered.slice(start, start + limit);
    
    return Response.json({
        products: paginated,
        pagination: {
            page,
            limit,
            total: filtered.length,
        },
    });
}

// POST handler
export async function POST(ctx: BurgerContext) {
    const { name, price, category } = ctx.validated?.body || {};
    
    const newProduct = {
        id: crypto.randomUUID(),
        name,
        price,
        category,
        createdAt: new Date().toISOString(),
    };
    
    return Response.json(newProduct, { status: 201 });
}
```

---
url: 'https://burger-api.com/docs/examples/complete-project'
---

# Complete Project Example

A complete project setup example.

## Project Structure

```
my-api/
├── src/
│   ├── index.ts
│   ├── api/
│   │   ├── route.ts
│   │   ├── users/
│   │   │   └── route.ts
│   │   └── users/
│   │       └── [id]/
│   │           └── route.ts
│   └── pages/
│       └── index.html
├── ecosystem/
│   └── hooks/
│       ├── cors/
│       └── logger/
├── package.json
└── tsconfig.json
```

## Main Server File

```typescript
// src/hooks.ts
import { cors } from '../ecosystem/hooks/cors/cors';
import { logger } from '../ecosystem/hooks/logger/logger';

export const onRequest = [logger()];
export const beforeRoute = [cors({ origin: '*' })];
```

```typescript
// src/index.ts
import { Burger } from 'burger-api';

const app = new Burger({
    apiDir: './src/api',
    apiPrefix: 'api',
    title: 'My API',
    description: 'API for managing users',
    version: '1.0.0',
    debug: process.env.NODE_ENV !== 'production',
});

const port = Number(process.env.PORT) || 4000;
await app.serve(port, () => {
    console.log(`Server running on http://localhost:${port}`);
});
```

## Example Route

```typescript
// src/api/users/schema.ts
import { z } from 'zod';

export const GET = {
    query: z.object({
        page: z.coerce.number().min(1).default(1),
    }),
};

export const POST = {
    body: z.object({
        name: z.string().min(1),
        email: z.string().email(),
    }),
};
```

```typescript
// src/api/users/route.ts
import type { BurgerContext } from 'burger-api';

export async function GET(ctx: BurgerContext) {
    const { page } = ctx.validated?.query || {};
    return Response.json({ users: [], page });
}

export async function POST(ctx: BurgerContext) {
    const { name, email } = ctx.validated?.body || {};
    return Response.json({ name, email }, { status: 201 });
}
```

## Running the Project

```bash
# Development
bun run dev

# Production
bun run start
```

---
url: 'https://burger-api.com/docs/advanced/performance'
---

# Performance Optimization

Burger API is optimized for performance with several built-in optimizations.

## Fast Paths

The framework uses specialized fast paths for common hook counts:
- 0 hooks - Ultra-fast path with no hook overhead
- 1 hook - Single hook fast path
- 2 hooks - Manual loop unrolling
- 3+ hooks - General path with pre-allocated arrays

## AOT Compilation

In production builds, the CLI discovers routes/pages at build time and generates a virtual entry with static imports.
That entry passes pre-built `apiRoutes` and `pageRoutes` into `new Burger(...)`, so runtime filesystem scanning is skipped.

## Route Matching

Routes are sorted by specificity:
1. Static routes (highest priority)
2. Dynamic routes
3. Wildcard routes (lowest priority)

This ensures the most specific route is matched first.

---
url: 'https://burger-api.com/docs/advanced/error-handling'
---

# Error Handling

Burger API provides comprehensive error handling.

## Automatic Error Handling

Errors in route handlers are automatically caught and returned as appropriate responses.

## Debug Mode

Enable debug mode for detailed error information:

```typescript
const app = new Burger({
    apiDir: './src/api',
    debug: true,
});
```

## Error Response Format

**Debug Mode Enabled:**
```json
{
    "request": "GET /api/users",
    "error": "Error message",
    "stack": "Error stack trace"
}
```

**Debug Mode Disabled:**
```
Internal Server Error
```

## Custom Error Handling

Handle errors in hooks:

```typescript
const errorHandler: Hook = async (ctx) => {
    try {
        return undefined; // Continue
    } catch (error) {
        return Response.json(
            { error: error.message },
            { status: 500 }
        );
    }
};
```

---
url: 'https://burger-api.com/docs/ecosystem/hooks-plugins'
---

# Available Hooks

Burger API ecosystem includes production-ready hooks.

## Core Hooks

- **cors** - Cross-Origin Resource Sharing
- **logger** - Request/response logging
- **rate-limiter** - Request rate limiting
- **jwt-auth** - JWT authentication
- **api-key-auth** - API key authentication
- **compression** - Response compression (gzip/deflate)
- **security-headers** - Security HTTP headers
- **cache** - HTTP caching headers
- **timeout** - Request timeout
- **body-size-limiter** - Request body size limits

## Installing Hooks

```bash
burger-api add cors logger rate-limiter
```

## Using Hooks

```typescript
// src/hooks.ts
import { cors } from './ecosystem/hooks/cors/cors';
import { logger } from './ecosystem/hooks/logger/logger';

export const onRequest = [logger()];
export const beforeRoute = [cors({ origin: '*' })];
```

Each hook includes a README with configuration options.

---
url: 'https://burger-api.com/docs/troubleshooting'
---

# Troubleshooting

Common issues and solutions.

## "burger-api: command not found"

**Solution:** Install the CLI using the install script:

```bash
# macOS/Linux
curl -fsSL https://burger-api.com/install.sh | bash

# Windows
irm https://burger-api.com/install.ps1 | iex
```

## "Directory already exists"

**Solution:** Choose a different project name:

```bash
burger-api create my-api-v2
```

## "Could not get hooks list from GitHub"

**Solution:** Check your internet connection. The CLI needs internet to download hooks.

## "Entry file not found: index.ts"

**Solution:** Make sure you're in the project directory:

```bash
cd my-project
burger-api dev
```

## Build fails with errors

**Solution:** Check that:
1. You're in a Burger API project directory
2. The entry file exists
3. There are no TypeScript errors

Run `bun run dev` first to see any errors.

## Cross-compilation fails from Windows (D:\ drive)

**Error:**
```
Failed to extract executable for 'bun-linux-x64-v1.3.4'. The download may be incomplete.
```

**Solution:** Move your project to `C:\` drive. Cross-compilation works correctly from the system drive.

---
url: 'https://burger-api.com/docs/getting-started'
---

# Getting Started

Quick start guide for Burger API.

## Installation

```bash
# Install Bun (if not already installed)
curl -fsSL https://bun.sh/install | bash

# Install burger-api
bun add burger-api
```

## Create Your First API

```typescript
// index.ts
import { Burger } from 'burger-api';

const app = new Burger({
    apiDir: './src/api',
});

await app.serve(4000);
```

## Create Your First Route

```typescript
// api/hello/route.ts
export async function GET() {
    return Response.json({ message: 'Hello, Burger API!' });
}
```

## Run Your Server

```bash
bun run index.ts
```

Visit `http://localhost:4000/api/hello` to see your API in action!

## Using the CLI

```bash
# Create a new project
burger-api create my-api

# Navigate to your project
cd my-api

# Start development server
bun run dev
```

---
url: 'https://burger-api.com/docs/quick-start'
---

# Quick Start

Get up and running with Burger API in 30 seconds.

## Step 1: Install CLI

```bash
curl -fsSL https://burger-api.com/install.sh | bash
```

## Step 2: Create Project

```bash
burger-api create my-api
```

## Step 3: Start Server

```bash
cd my-api
bun run dev
```

## Step 4: Open Browser

Visit `http://localhost:4000` to see your API!

## Next Steps

- Add hooks: `burger-api add cors logger`
- Create routes in `src/api/`
- View API docs at `http://localhost:4000/docs`
- Check OpenAPI spec at `http://localhost:4000/openapi.json`

That's it! Your Burger API server is running! 🎉

